[user]
    email = me@weastur.com
    name = Pavel Sapezhka
    signingkey = C1FC1D01F90412D53941648C21B982BF130202F7
[branch]
    autosetuprebase = always
[push]
    default = simple
    autoSetupRemote = true
[core]
    editor = vim
    excludesfile = ~/.gitignore
[diff]
    tool = vimdiff
    renameLimit = 1833
[alias]
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    di = diff
    br = branch
    ci = commit
    st = status
    co = checkout
    ps = push
    ssave = stash save
    spop = stash pop
[filter "lfs"]
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
[gpg]
    program = gpg
[commit]
    gpgsign = true
[tag]
    forceSignAnnotated = true
[remote "origin"]
    prune = true
[init]
    defaultBranch = main
[includeIf "gitdir:/Users/weastur/src/w/"]
    path = .gitconfig-easybrain
